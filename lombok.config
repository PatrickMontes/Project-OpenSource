# Copia @Lazy de los campos a los parametros del constructor generado por
# @RequiredArgsConstructor, para que Spring pueda romper las dependencias
# circulares entre servicios (PlanillaService <-> CalculoFinancieroService).
config.stopBubbling = true
lombok.copyableAnnotations += org.springframework.context.annotation.Lazy
